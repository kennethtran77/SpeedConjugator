import React from 'react';

class Verbs extends React.Component {
    render() {
        return (
            <div id="dropdown">
                <h2>Verbs</h2>
            </div>
        );
    }
}

export default Verbs;